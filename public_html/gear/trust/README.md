This directory stores trust anchors and certificate bundles used for server-side verification.

Recommended paths (outside the web root):
- `/data/trust/passport-csca.pem` (CSCA bundle for ICAO passport passive authentication)

Do not store private keys here.

