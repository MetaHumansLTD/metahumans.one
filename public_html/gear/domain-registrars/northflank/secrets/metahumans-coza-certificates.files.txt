Create the `metahumans-coza-certificates` mounted secret set with files such as:

- client.pem
- ca.pem

If the registrar issues a `.p12` or `.pfx` bundle, mount that file instead and point `COZA_CERT_PATH` at it.
